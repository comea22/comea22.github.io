---
interface Props {
	image: string;
	title: string;
	description: string;
	alt?: string;
}

const { image, title, description, alt = title } = Astro.props;
---

<div class="rounded-lg overflow-hidden shadow-md bg-white h-54">
	<img src={image} alt={alt} class="w-full h-36 object-cover" />
	<div class="p-4">
		<h3 class="text-lg font-bold text-primary">{title}</h3>
		<p class="text-gray-dark text-sm mt-2">{description}</p>
	</div>
</div>
